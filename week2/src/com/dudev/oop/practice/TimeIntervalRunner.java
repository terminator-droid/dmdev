package com.dudev.oop.practice;

public class TimeIntervalRunner {
    public static void main(String[] args) {
        TimeInterval timeInterval = new TimeInterval(3923);
        System.out.println(timeInterval.hours + " " + timeInterval.minutes + " " + timeInterval.seconds);
    }
}
